# 🚀 Git Commands Guide

## Where to Run Git Commands:

### Method 1: Current Folder CMD
1. Open CMD in your project folder: `C:\Users\osman\OneDrive\Desktop\Mobile-ERP`
2. Type: `git push origin main`

### Method 2: VS Code Terminal
1. Open VS Code
2. Terminal → New Terminal (Ctrl + `)
3. Type: `git push origin main`

### Method 3: Git Bash
1. Right-click in project folder
2. "Git Bash Here"
3. Type: `git push origin main`

## Quick Commands:
```bash
# Check status
git status

# Add all changes
git add .

# Commit changes
git commit -m "Your message"

# Push to website
git push origin main
```

## Auto-Deploy Process:
1. You make changes locally
2. Run: `git push origin main`
3. GitHub updates automatically
4. Render deploys to www.bizpulse24.com
5. Wait 2-3 minutes for live update